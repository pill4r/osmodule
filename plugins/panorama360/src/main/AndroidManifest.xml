<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="@string/panorama_plugin_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <activity
            android:name=".PanoramaPluginHomeActivity"
            android:exported="true"
            android:permission="dev.pillar.osmodule.permission.BIND_PLUGIN">
            <intent-filter>
                <action android:name="dev.pillar.osmodule.plugin.MANAGE_PERMISSIONS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Synchronous first-launch IPC; only the same-signature Base APK may call it. -->
        <provider
            android:name="dev.pillar.osmodule.plugin.PluginBootstrapProvider"
            android:authorities="${applicationId}.bootstrap"
            android:exported="true"
            android:permission="dev.pillar.osmodule.permission.BIND_PLUGIN"
            android:grantUriPermissions="false" />

        <service
            android:name=".PanoramaPluginService"
            android:exported="true"
            android:permission="dev.pillar.osmodule.permission.BIND_PLUGIN">
            <intent-filter>
                <action android:name="dev.pillar.osmodule.plugin.BIND" />
            </intent-filter>
            <meta-data android:name="dev.pillar.osmodule.plugin.ID" android:value="panorama360" />
            <meta-data android:name="dev.pillar.osmodule.plugin.NAME" android:value="osmodule 360 Viewer" />
            <meta-data android:name="dev.pillar.osmodule.plugin.VERSION" android:value="3" />
            <meta-data android:name="dev.pillar.osmodule.plugin.PROTOCOL_MIN" android:value="1" />
            <meta-data android:name="dev.pillar.osmodule.plugin.PROTOCOL_MAX" android:value="1" />
            <meta-data
                android:name="dev.pillar.osmodule.plugin.CAPABILITIES"
                android:value="camera.media.360-view" />
        </service>
    </application>
</manifest>
