<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="dev.konraditurbe.osmosis.permission.BIND_PLUGIN"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="dev.konraditurbe.osmosis.permission.BIND_PLUGIN" />

    <queries>
        <package android:name="dev.konraditurbe.osmosis.plugin.rsdk" />
        <intent>
            <action android:name="dev.konraditurbe.osmosis.plugin.BIND" />
        </intent>
        <intent>
            <action android:name="dev.konraditurbe.osmosis.plugin.MANAGE_PERMISSIONS" />
        </intent>
        <provider android:authorities="dev.konraditurbe.osmosis.plugin.rsdk.bootstrap" />
    </queries>

    <application
        android:name=".OsModuleApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.OsModule">

        <activity
            android:name=".plugins.PluginManagerActivity"
            android:exported="false" />
    </application>
</manifest>
